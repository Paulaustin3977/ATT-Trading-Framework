# Example environment variables for the ATT website build.
# All variables are optional. The build, run, and deploy flows all work with defaults.

# Path to the framework repository containing /docs, /specifications,
# /research/Reports/RDR, /pine/releases and release manifests.
# Default: ../ATT-Trading-Framework
# ATT_REPO_PATH=/path/to/ATT-Trading-Framework
